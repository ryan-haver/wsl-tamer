<ui:FluentWindow x:Class="WslTamer.UI.MoveDistroWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WslTamer.UI"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Move Distribution" Height="200" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Install Location -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <Label Content="New Install Location" Padding="0,0,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ui:TextBox Name="TxtLocation" Grid.Column="0" Margin="0,0,5,0" ToolTip="Select the new folder where the distribution will be moved."/>
                <ui:Button Content="..." Grid.Column="1" Width="30" Click="BtnBrowseLocation_Click" ToolTip="Browse for folder."/>
            </Grid>
            <TextBlock Text="Warning: This process involves exporting, unregistering, and re-importing the distribution. Do not interrupt it." 
                       Foreground="Orange" TextWrapping="Wrap" Margin="0,5,0,0" FontSize="11"/>
        </StackPanel>

        <!-- Progress -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" Name="PnlProgress" Visibility="Collapsed">
            <ProgressBar IsIndeterminate="True" Height="10" Margin="0,0,0,5"/>
            <TextBlock Text="Moving... This may take a while." HorizontalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <ui:Button Content="Cancel" Width="80" Margin="0,0,10,0" Click="BtnCancel_Click" Name="BtnCancel"/>
            <ui:Button Content="Move" Width="80" Click="BtnMove_Click" Name="BtnMove" IsDefault="True" ToolTip="Start the move process." Appearance="Primary"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>