<ui:FluentWindow x:Class="WslTamer.UI.DistroRegistryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Distro Registry" Height="450" Width="600"
        MinHeight="400" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Available Distributions" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        <TextBlock Text="Select a distribution to install from the official online registry." Margin="0,0,0,10" Grid.Row="0" VerticalAlignment="Bottom" HorizontalAlignment="Right" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>

        <DataGrid Name="DgDistros" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Friendly Name" Binding="{Binding State}" Width="*"/>
                <DataGridTemplateColumn Header="Action" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ui:Button Content="Install" Click="BtnInstall_Click" Tag="{Binding Name}" Appearance="Primary"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <ui:Button Content="Close" Click="BtnClose_Click" MinWidth="80"/>
        </StackPanel>
        
        <ProgressBar Name="PbLoading" Grid.Row="1" IsIndeterminate="True" Width="200" Height="4" VerticalAlignment="Center" Visibility="Visible"/>
    </Grid>
</ui:FluentWindow>
