<Window x:Class="WslTamer.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:local="clr-namespace:WslTamer.UI"
        mc:Ignorable="d"
        Title="WSL Tamer" Height="0" Width="0"
        ShowInTaskbar="False"
        Visibility="Hidden"
        WindowStyle="None"
        ResizeMode="NoResize"
        Icon="/Assets/app.ico">
    
    <Window.Resources>
        <!-- Dark Theme ContextMenu Style -->
        <Style TargetType="{x:Type ContextMenu}">
            <Setter Property="Background" Value="#202020"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark Theme MenuItem Style -->
        <Style TargetType="{x:Type MenuItem}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="4" Margin="0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                                    <ColumnDefinition Width="15"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" ContentSource="Icon" HorizontalAlignment="Center" VerticalAlignment="Center" Width="16" Height="16" Margin="0,0,12,0"/>
                                <ContentPresenter Grid.Column="1" ContentSource="Header" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="{TemplateBinding InputGestureText}" VerticalAlignment="Center" Foreground="#888888" Margin="10,0,0,0"/>
                                <Path Grid.Column="3" x:Name="Arrow" Data="M0,0 L4,3.5 L0,7 Z" Fill="#E0E0E0" HorizontalAlignment="Right" VerticalAlignment="Center" Visibility="Collapsed"/>
                                
                                <Popup x:Name="Popup" Placement="Right" IsOpen="{TemplateBinding IsSubmenuOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                    <Border Background="#202020" BorderBrush="#333333" BorderThickness="1" CornerRadius="8" Padding="4" Margin="4,0,0,0">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#353535"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="True">
                                <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type Separator}">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Separator}">
                        <Border Background="{TemplateBinding Background}" Height="1" SnapsToDevicePixels="true"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <tb:TaskbarIcon x:Name="MyNotifyIcon"
                        ToolTipText="WSL Tamer"
                        IconSource="/Assets/app.ico">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu x:Name="TrayMenu">
                    <MenuItem Header="Status: Checking..." x:Name="StatusMenuItem" IsEnabled="False" FontWeight="Bold"/>
                    <Separator/>
                    <MenuItem Header="Profiles" x:Name="ProfilesMenu"/>
                    <MenuItem Header="Actions">
                        <MenuItem Header="Open Terminal" Click="LaunchWsl_Click"/>
                        <MenuItem Header="Start Background" Click="StartBackground_Click"/>
                        <MenuItem Header="Shutdown WSL" Click="ShutdownWsl_Click"/>
                        <MenuItem Header="Reclaim Memory" Click="ReclaimMemory_Click"/>
                    </MenuItem>
                    <Separator/>
                    <MenuItem Header="Mounted Devices" x:Name="MountedDevicesMenu" Visibility="Collapsed"/>
                    <Separator x:Name="MountedDevicesSeparator" Visibility="Collapsed"/>
                    <MenuItem Header="Settings" Click="Settings_Click"/>
                    <MenuItem Header="Start with Windows" x:Name="TrayStartWithWindows" IsCheckable="True" Click="TrayStartWithWindows_Click"/>
                    <MenuItem Header="Exit" Click="Exit_Click"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Grid>
</Window>
