<UserControl x:Class="WslTamer.UI.Views.ProfilesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="HelpIcon" TargetType="ui:SymbolIcon">
            <Setter Property="Symbol" Value="Info24"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Help"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="0,10,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Profile List -->
        <GroupBox Header="Profiles" Grid.Column="0" Margin="0,0,5,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,0">
                    <Button Name="BtnAddProfile" Content="Add" Margin="0,0,10,0" Click="BtnAddProfile_Click"/>
                    <Button Name="BtnRemoveProfile" Content="Remove" Click="BtnRemoveProfile_Click"/>
                </StackPanel>
                <ListBox Name="LstProfiles" DisplayMemberPath="Name" SelectionChanged="LstProfiles_SelectionChanged"
                         AllowDrop="True"
                         PreviewMouseLeftButtonDown="LstProfiles_PreviewMouseLeftButtonDown"
                         Drop="LstProfiles_Drop"
                         MouseMove="LstProfiles_MouseMove"/>
            </DockPanel>
        </GroupBox>
        
        <!-- Profile Editor -->
        <Grid Grid.Column="1" Margin="10,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Name="PnlEditor" IsEnabled="False">
                    <GroupBox Header="Settings" Margin="0,0,0,10">
                        <StackPanel Margin="5">
                            <Label Content="Profile Name"/>
                            <TextBox Name="TxtName" Margin="0,0,0,10"/>
                            
                            <StackPanel Orientation="Horizontal">
                                <Label Content="Memory Limit"/>
                                <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Maximum RAM allocated to the WSL 2 VM. Examples: 4GB, 512MB."/>
                            </StackPanel>
                            <TextBox Name="TxtMemory" Margin="0,0,0,10"/>
                            
                            <StackPanel Orientation="Horizontal">
                                <Label Content="Processors"/>
                                <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Number of CPU cores assigned to WSL. Set to 0 for default (all cores)."/>
                            </StackPanel>
                            <TextBox Name="TxtProcessors" Margin="0,0,0,10"/>
                            
                            <StackPanel Orientation="Horizontal">
                                <Label Content="Swap"/>
                                <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Virtual memory size (e.g., 2GB). Set to 0 to disable swap."/>
                            </StackPanel>
                            <TextBox Name="TxtSwap" Margin="0,0,0,10"/>
                            
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <CheckBox Name="ChkLocalhost" Content="Localhost Forwarding"/>
                                <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Access WSL network services from Windows via localhost."/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <CheckBox Name="ChkDefaultProfile" Content="Set as Default Automation Profile"/>
                                <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="This profile will be applied when no automation rules are active."/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Advanced Global Settings (.wslconfig)" Margin="0,0,0,10">
                        <StackPanel Margin="5">
                            <StackPanel Orientation="Horizontal">
                                <Label Content="Kernel Path"/>
                                <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Path to a custom Linux kernel image. Leave empty for default."/>
                            </StackPanel>
                            <TextBox Name="TxtKernelPath" Margin="0,0,0,5"/>

                            <StackPanel Orientation="Horizontal">
                                <Label Content="Networking Mode"/>
                                <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="NAT (Default), Mirrored (New in WSL2), or Bridged."/>
                            </StackPanel>
                            <ComboBox Name="CboNetworkingMode" Margin="0,0,0,5">
                                <ComboBoxItem Content="NAT"/>
                                <ComboBoxItem Content="Mirrored"/>
                                <ComboBoxItem Content="Bridged"/>
                            </ComboBox>

                            <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                <CheckBox Name="ChkGuiApplications" Content="Enable GUI Applications (WSLg)"/>
                                <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Enable support for Linux GUI apps (WSLg)."/>
                            </StackPanel>
                            
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <CheckBox Name="ChkDebugConsole" Content="Show Debug Console"/>
                                <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Show the WSL2 kernel debug console on startup."/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Auto-Switch Triggers" Margin="0,10,0,10">
                        <StackPanel Margin="5">
                            <TextBlock Text="Automatically apply this profile when:" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,5" FontStyle="Italic"/>
                            <ListBox Name="LstTriggers" Height="80" Margin="0,0,0,5">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Description}" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <ComboBox Name="CboTriggerType" Width="140" Margin="0,0,5,0" SelectedIndex="0">
                                    <ComboBoxItem Content="Process Start" ToolTip="Trigger when a specific process starts (e.g. code.exe)"/>
                                    <ComboBoxItem Content="Connected to Network" ToolTip="Trigger when connected to a specific Wi-Fi SSID"/>
                                </ComboBox>
                                <TextBox Name="TxtTriggerValue" Width="120" Margin="0,0,5,0" ToolTip="Value (e.g. 'code.exe' or 'MyHomeWiFi')"/>
                                <Button Content="Add" Width="50" Click="BtnAddTrigger_Click"/>
                                <Button Content="Del" Width="50" Margin="5,0,0,0" Click="BtnDeleteTrigger_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>
                    
                    <Button Content="Save Changes" HorizontalAlignment="Right" MinWidth="120" Click="BtnSaveProfile_Click"/>
                </StackPanel>
            </ScrollViewer>
            
            <!-- Empty State Overlay -->
            <Border Name="PnlNoProfile" Background="{DynamicResource ApplicationBackgroundBrush}" Visibility="Visible">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Select a profile to configure" 
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                               FontSize="16" 
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="or create a new one" 
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                               FontSize="12" 
                               HorizontalAlignment="Center"
                               Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
