<ui:FluentWindow x:Class="WslTamer.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WslTamer.UI"
        xmlns:converters="clr-namespace:WslTamer.UI.Converters"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="WSL Tamer Settings" Height="550" Width="700"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:StateToColorConverter x:Key="StateToColorConverter"/>
        <converters:BooleanToAttachConverter x:Key="BooleanToAttachConverter"/>
        
        <Style x:Key="HelpIcon" TargetType="ui:SymbolIcon">
            <Setter Property="Symbol" Value="Info24"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Help"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="WSL Tamer Settings" Icon="/Assets/app.ico" />

        <TabControl Grid.Row="1" Margin="10">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="MinWidth" Value="0"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="HorizontalAlignment" Value="Left"/>
                </Style>
            </TabControl.ItemContainerStyle>
            <TabItem Header="General">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <CheckBox Name="ChkStartOnLogin" Content="Start WSL Tamer when I log in" Click="ChkStartOnLogin_Click"/>
                            <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Launch the application automatically on Windows startup."/>
                        </StackPanel>
                        
                        <GroupBox Header="Global Actions" Margin="0,10,0,0">
                            <WrapPanel Margin="5">
                                <Button Content="Open Terminal" Margin="0,0,10,10" Click="BtnLaunchWsl_Click"
                                        ToolTip="Open the default WSL distribution."/>
                                <Button Content="Start Background" Margin="0,0,10,10" Click="BtnStartBackground_Click"
                                        ToolTip="Start WSL without a window to keep background services running."/>
                                <Button Content="Shutdown WSL" Margin="0,0,10,10" Click="BtnShutdownWsl_Click"
                                        ToolTip="Immediately stop all running WSL distributions."/>
                                <Button Content="Reclaim Memory" Margin="0,0,10,10" Click="BtnReclaimMemory_Click"
                                        ToolTip="Force Linux to drop caches and return unused RAM to Windows."/>
                                <Button Content="Kill All WSL" Margin="0,0,10,10" Click="BtnKillAll_Click"
                                        ToolTip="Forcefully terminate all WSL processes and the service."/>
                                <Button Content="Open Explorer" Margin="0,0,10,10" Click="BtnOpenExplorer_Click"
                                        ToolTip="Open File Explorer to the WSL network share."/>
                            </WrapPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Distributions">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
                        <Button Content="Install New" Click="BtnInstallDistro_Click" Margin="0,0,10,0" ToolTip="Install a new distribution from the online registry."/>
                        <Button Content="Import Distro" Click="BtnImportDistro_Click" Margin="0,0,10,0" ToolTip="Import a distribution from a .tar file."/>
                        <Button Content="Refresh" Click="BtnRefreshDistros_Click" ToolTip="Reload the list of distributions."/>
                    </StackPanel>
                    
                    <ScrollViewer Grid.Row="1">
                        <ItemsControl Name="IcDistros">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <ui:Card Margin="5" Width="320" Padding="10">
                                        <StackPanel>
                                            <DockPanel LastChildFill="False">
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                                                <TextBlock Text="★" Visibility="{Binding IsDefault, Converter={StaticResource BooleanToVisibilityConverter}}" DockPanel.Dock="Right" Foreground="Gold" ToolTip="Default Distribution"/>
                                            </DockPanel>
                                            <TextBlock Text="{Binding State}" Margin="0,5,0,0" Foreground="{Binding IsRunning, Converter={StaticResource StateToColorConverter}}"/>
                                            <TextBlock Text="{Binding Version, StringFormat='WSL {0}'}" Margin="0,0,0,10" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                            
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,5">
                                                <Button Content="Run" Margin="0,0,5,0" Tag="{Binding Name}" Click="BtnRunDistro_Click" ToolTip="Start this distribution in a new terminal window."/>
                                                <Button Content="Stop" Margin="0,0,5,0" Tag="{Binding Name}" Click="BtnStopDistro_Click" ToolTip="Terminate this distribution."/>
                                                <Button Content="Settings" Margin="0,0,5,0" Tag="{Binding Name}" Click="BtnDistroSettings_Click" ToolTip="Configure wsl.conf for this distribution."/>
                                                <Button Content="Set Default" Tag="{Binding Name}" Click="BtnSetDefaultDistro_Click" ToolTip="Make this the default WSL distribution."/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                <Button Content="Clone" Margin="0,0,5,0" Tag="{Binding Name}" Click="BtnCloneDistro_Click" ToolTip="Create a copy of this distribution."/>
                                                <Button Content="Move" Margin="0,0,5,0" Tag="{Binding Name}" Click="BtnMoveDistro_Click" ToolTip="Move this distribution to another drive or folder."/>
                                                <Button Content="Export" Margin="0,0,5,0" Tag="{Binding Name}" Click="BtnExportDistro_Click" ToolTip="Backup this distribution to a .tar file."/>
                                                <Button Content="Unregister" Tag="{Binding Name}" Click="BtnUnregisterDistro_Click" Background="#33FF0000" BorderBrush="Red" ToolTip="Permanently delete this distribution."/>
                                            </StackPanel>
                                        </StackPanel>
                                    </ui:Card>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </TabItem>
            <TabItem Header="Profiles">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Profile List -->
                    <DockPanel Grid.Column="0">
                        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,0">
                            <Button Name="BtnAddProfile" Content="Add" Margin="0,0,10,0" Click="BtnAddProfile_Click"/>
                            <Button Name="BtnRemoveProfile" Content="Remove" Click="BtnRemoveProfile_Click"/>
                        </StackPanel>
                        <ListBox Name="LstProfiles" DisplayMemberPath="Name" SelectionChanged="LstProfiles_SelectionChanged"
                                 AllowDrop="True"
                                 PreviewMouseLeftButtonDown="LstProfiles_PreviewMouseLeftButtonDown"
                                 Drop="LstProfiles_Drop"
                                 MouseMove="LstProfiles_MouseMove"/>
                    </DockPanel>
                    
                    <!-- Profile Editor -->
                    <Grid Grid.Column="1" Margin="10,0,0,0">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Name="PnlEditor" IsEnabled="False">
                                <Label Content="Profile Name"/>
                                <TextBox Name="TxtName" Margin="0,0,0,10"/>
                                
                                <StackPanel Orientation="Horizontal">
                                    <Label Content="Memory Limit"/>
                                    <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Maximum RAM allocated to the WSL 2 VM. Examples: 4GB, 512MB."/>
                                </StackPanel>
                                <TextBox Name="TxtMemory" Margin="0,0,0,10"/>
                                
                                <StackPanel Orientation="Horizontal">
                                    <Label Content="Processors"/>
                                    <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Number of CPU cores assigned to WSL. Set to 0 for default (all cores)."/>
                                </StackPanel>
                                <TextBox Name="TxtProcessors" Margin="0,0,0,10"/>
                                
                                <StackPanel Orientation="Horizontal">
                                    <Label Content="Swap"/>
                                    <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Virtual memory size (e.g., 2GB). Set to 0 to disable swap."/>
                                </StackPanel>
                                <TextBox Name="TxtSwap" Margin="0,0,0,10"/>
                                
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <CheckBox Name="ChkLocalhost" Content="Localhost Forwarding"/>
                                    <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Access WSL network services from Windows via localhost."/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <CheckBox Name="ChkDefaultProfile" Content="Set as Default Automation Profile"/>
                                    <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="This profile will be applied when no automation rules are active."/>
                                </StackPanel>

                                <GroupBox Header="Advanced Global Settings (.wslconfig)" Margin="0,0,0,10">
                                    <StackPanel Margin="5">
                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="Kernel Path"/>
                                            <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Path to a custom Linux kernel image. Leave empty for default."/>
                                        </StackPanel>
                                        <TextBox Name="TxtKernelPath" Margin="0,0,0,5"/>

                                        <StackPanel Orientation="Horizontal">
                                            <Label Content="Networking Mode"/>
                                            <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="NAT (Default), Mirrored (New in WSL2), or Bridged."/>
                                        </StackPanel>
                                        <ComboBox Name="CboNetworkingMode" Margin="0,0,0,5">
                                            <ComboBoxItem Content="NAT"/>
                                            <ComboBoxItem Content="Mirrored"/>
                                            <ComboBoxItem Content="Bridged"/>
                                        </ComboBox>

                                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                            <CheckBox Name="ChkGuiApplications" Content="Enable GUI Applications (WSLg)"/>
                                            <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Enable support for Linux GUI apps (WSLg)."/>
                                        </StackPanel>
                                        
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                            <CheckBox Name="ChkDebugConsole" Content="Show Debug Console"/>
                                            <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="Show the WSL2 kernel debug console on startup."/>
                                        </StackPanel>
                                    </StackPanel>
                                </GroupBox>

                                <GroupBox Header="Auto-Switch Triggers" Margin="0,10,0,10">
                                    <StackPanel Margin="5">
                                        <TextBlock Text="Automatically apply this profile when:" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,5" FontStyle="Italic"/>
                                        <ListBox Name="LstTriggers" Height="80" Margin="0,0,0,5">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Description}" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <ComboBox Name="CboTriggerType" Width="140" Margin="0,0,5,0" SelectedIndex="0">
                                                <ComboBoxItem Content="Process Start" ToolTip="Trigger when a specific process starts (e.g. code.exe)"/>
                                                <ComboBoxItem Content="Connected to Network" ToolTip="Trigger when connected to a specific Wi-Fi SSID"/>
                                            </ComboBox>
                                            <TextBox Name="TxtTriggerValue" Width="120" Margin="0,0,5,0" ToolTip="Value (e.g. 'code.exe' or 'MyHomeWiFi')"/>
                                            <Button Content="Add" Width="50" Click="BtnAddTrigger_Click"/>
                                            <Button Content="Del" Width="50" Margin="5,0,0,0" Click="BtnDeleteTrigger_Click"/>
                                        </StackPanel>
                                    </StackPanel>
                                </GroupBox>
                                
                                <Button Content="Save Changes" HorizontalAlignment="Right" MinWidth="120" Click="BtnSaveProfile_Click"/>
                            </StackPanel>
                        </ScrollViewer>
                        
                        <!-- Empty State Overlay -->
                        <Border Name="PnlNoProfile" Background="{DynamicResource ApplicationBackgroundBrush}" Visibility="Visible">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="Select a profile to configure" 
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                                           FontSize="16" 
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="or create a new one" 
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                                           FontSize="12" 
                                           HorizontalAlignment="Center"
                                           Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>
            
            <TabItem Header="Hardware">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <!-- USB Devices -->
                        <GroupBox Header="USB Devices (Requires usbipd-win)" Margin="0,0,0,10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="200"/>
                                </Grid.RowDefinitions>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                    <Button Content="Refresh USB" Click="BtnRefreshUsb_Click" Margin="0,0,10,0" ToolTip="Scan for connected USB devices."/>
                                    <TextBlock Name="TxtUsbStatus" VerticalAlignment="Center" Foreground="Gray"/>
                                    <TextBlock Name="TxtUsbInstallLink" VerticalAlignment="Center" Margin="10,0,0,0" Visibility="Collapsed"
                                               ToolTip="After installing usbipd-win, you must restart WSL Tamer for changes to take effect.">
                                        <Hyperlink NavigateUri="https://github.com/dorssel/usbipd-win/releases" RequestNavigate="Hyperlink_RequestNavigate">
                                            Install usbipd-win
                                        </Hyperlink>
                                    </TextBlock>
                                </StackPanel>
                                <DataGrid Name="DgUsbDevices" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Bus ID" Binding="{Binding BusId}" Width="60"/>
                                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                                        <DataGridTextColumn Header="State" Binding="{Binding State}" Width="100"/>
                                        <DataGridTemplateColumn Header="Action" Width="100">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding IsAttached, Converter={StaticResource BooleanToAttachConverter}}" 
                                                            Click="BtnToggleUsb_Click" 
                                                            Tag="{Binding BusId}"
                                                            ToolTip="Attach/Detach this USB device to/from WSL."/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </GroupBox>

                        <!-- Physical Disks -->
                        <GroupBox Header="Physical Disks" Margin="0,0,0,10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="150"/>
                                </Grid.RowDefinitions>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                    <Button Content="Refresh Disks" Click="BtnRefreshDisks_Click" Margin="0,0,10,0" ToolTip="Scan for physical disks."/>
                                    <Label Content="Distro:" VerticalAlignment="Center"/>
                                    <ComboBox Name="CboDiskDistro" Width="120" Margin="5,0,10,0" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                    <TextBlock Text="Warning: Mounting disks requires Admin privileges." VerticalAlignment="Center" Foreground="Orange"/>
                                </StackPanel>
                                <DataGrid Name="DgDisks" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Device ID" Binding="{Binding DeviceId}" Width="150"/>
                                        <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="*"/>
                                        <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="80"/>
                                        <DataGridTemplateColumn Header="Action" Width="100">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="Mount" Click="BtnMountDisk_Click" Tag="{Binding DeviceId}" ToolTip="Mount this physical disk into WSL."/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Header="Action" Width="100">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="Unmount" Click="BtnUnmountDisk_Click" Tag="{Binding DeviceId}" ToolTip="Unmount this disk from WSL."/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </GroupBox>

                        <!-- Folder Mounts -->
                        <GroupBox Header="Folder Mounts (drvfs)" Margin="0,0,0,10">
                            <StackPanel Margin="5">
                                <TextBlock Text="Mount a Windows folder into a specific WSL distribution." Margin="0,0,0,5"/>
                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Label Content="Distro:" VerticalAlignment="Center" Width="100"/>
                                    <ComboBox Name="CboMountDistro" Grid.Column="1" Margin="5,0" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                    <Button Content="Refresh" Grid.Column="2" Click="BtnRefreshMountDistros_Click" Padding="5,2"/>
                                </Grid>
                                
                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Label Content="Windows Path:" VerticalAlignment="Center" Width="100"/>
                                    <TextBox Name="TxtWindowsPath" Grid.Column="1" Margin="5,0" VerticalContentAlignment="Center"/>
                                    <Button Content="Browse..." Grid.Column="2" Click="BtnBrowseFolder_Click" Padding="5,2"/>
                                </Grid>

                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center" Width="100">
                                        <Label Content="WSL Path:" Padding="0"/>
                                        <ui:SymbolIcon Style="{StaticResource HelpIcon}" ToolTip="The path inside Linux where the folder will appear (e.g. /mnt/myfolder)."/>
                                    </StackPanel>
                                    <TextBox Name="TxtWslPath" Grid.Column="1" Margin="5,0" VerticalContentAlignment="Center"/>
                                </Grid>

                                <Button Content="Mount Folder" HorizontalAlignment="Right" Click="BtnMountFolder_Click" MinWidth="120"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- PCIe Passthrough -->
                        <GroupBox Header="PCIe Passthrough (Advanced - DDA)">
                            <StackPanel Margin="5">
                                <TextBlock Text="Discrete Device Assignment (DDA) allows passing PCIe devices to WSL." TextWrapping="Wrap" Margin="0,0,0,5"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="Requirements:" FontWeight="Bold" Margin="0,0,5,0"/>
                                    <TextBlock Text="SR-IOV" TextDecorations="Underline" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,10,0" Cursor="Help"
                                               ToolTip="Single Root I/O Virtualization (SR-IOV) must be enabled in your BIOS/UEFI settings."/>
                                    <TextBlock Text="IOMMU" TextDecorations="Underline" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,10,0" Cursor="Help"
                                               ToolTip="IOMMU (Intel VT-d or AMD-Vi) must be enabled in your BIOS/UEFI settings."/>
                                    <TextBlock Text="Admin" TextDecorations="Underline" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Cursor="Help"
                                               ToolTip="Requires Administrator privileges to detach devices from Windows."/>
                                </StackPanel>
                                <TextBlock Text="Warning: Detaching a device (e.g. GPU) from Windows may cause instability." Foreground="Orange" TextWrapping="Wrap" Margin="0,0,0,5"/>
                                <Button Content="Manage PCIe Devices (Coming Soon)" IsEnabled="False" HorizontalAlignment="Left" ToolTip="Future feature: Manage DDA devices."/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            
            <TabItem Header="About">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <TextBlock Text="WSL Tamer" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Name="TxtVersion" Text="Version 1.0.0" FontSize="14" Margin="0,0,0,20"/>
                        
                        <GroupBox Header="Updates">
                            <StackPanel Margin="5">
                                <CheckBox Name="ChkAutoCheckUpdates" Content="Automatically check for updates on startup" IsChecked="True" Margin="0,0,0,10" ToolTip="Enable automatic update checks when the application starts."/>
                                <Button Content="Check for Updates" HorizontalAlignment="Left" MinWidth="120" Click="BtnCheckUpdates_Click" ToolTip="Check for updates immediately."/>
                                <TextBlock Name="TxtUpdateStatus" Margin="0,10,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</ui:FluentWindow>
